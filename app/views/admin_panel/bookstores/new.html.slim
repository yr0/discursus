.box.box-success
  .box-header.with-border
    h2 = t('admin_panel.bookstores.new_bookstore')
  .box-body
    .row
      = simple_form_for [:admin_panel, @bookstore], html: { class: 'form' } do |f|
        = render('form', f: f)
